import React from "react"
import { SquareWrapper } from "../../styled/SqContentCard"

const SqWrapper = ({ lastThreeCards }) => {
  return <SquareWrapper>{lastThreeCards}</SquareWrapper>
}

export default SqWrapper
